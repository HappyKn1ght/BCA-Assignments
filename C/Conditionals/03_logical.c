#include <stdio.h>

int main()
{
    int a = 1;
    int b = 1;
    printf("The Value of a AND b is %d\n", a && b);
    printf("The Value of a OR b is %d\n", a || b);
    printf("The Value of NOT(a) is %d", !a);

    return 0;
}